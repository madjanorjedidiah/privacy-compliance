# Local dev overlay: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up`
services:
  web:
    environment:
      DJANGO_ENV: dev
      DJANGO_DEBUG: "1"
      SENTINEL_SECRET_KEY: django-insecure-dev-only-change-in-prod-wpfz7-4gc4pgimbcp
    ports:
      - "8000:8000"
  nginx:
    profiles: ["disabled"]
